# AGENTS.md

The project guide lives in **[CLAUDE.md](CLAUDE.md)** — quick start, the rules
that apply everywhere, and an index into `docs/`.

This file used to be a second copy of it and had drifted a long way out of date
(five grayscale swatches, no pattern brush, no layers). It is a pointer now, so
there is only ever one source of truth.
